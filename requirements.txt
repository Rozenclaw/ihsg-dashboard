streamlit>=1.33
yfinance>=0.2.40
pandas>=2.1
numpy>=1.24
plotly>=5.20
PyYAML>=6.0
requests>=2.31
urllib3<2
watchdog>=4.0
# Note: the optional AI narrative (Decision Helper) needs no extra package — it
# calls any OpenAI-compatible provider (Gemini/Groq/OpenRouter/Ollama) over HTTP
# via `requests`. Just set the provider's API key; see config.yaml `ai`.
