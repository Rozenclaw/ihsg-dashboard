__pycache__/
*.pyc
data/*.db
data/*.db.bak-*
data/*.db-shm
data/*.db-wal
# Committed for cloud deploy: the full IDX ticker list + the gzipped seed snapshot
# (uncompressed data/seed.db stays ignored above via data/*.db).
!data/universe.csv
.venv/
.streamlit/secrets.toml
.streamlit/*.log

# Claude Code machine-local settings
.claude/settings.local.json
